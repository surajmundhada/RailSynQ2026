fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==23.0.0
pydantic==2.8.2
orjson==3.10.7
python-dotenv==1.0.1
SQLAlchemy==2.0.32
aiosqlite==0.20.0
redis==5.0.8
websockets==12.0
# Use pure-Python stack to avoid building cryptography on Render Free
python-jose==3.3.0
ecdsa==0.19.0
pyasn1==0.6.0
passlib[bcrypt]==1.7.4

# NOTE: This server-only requirements file intentionally omits heavy ML libs
# like torch, ray[rllib], and ortools to keep build size/RAM low on Render.
# The API code paths do not import those packages at startup.

